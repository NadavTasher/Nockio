user nginx;

worker_processes 1;

pid /var/run/nginx.pid;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        listen 443 ssl;
    }
}

include /var/lib/nockio/proxy/configurations/*.conf;